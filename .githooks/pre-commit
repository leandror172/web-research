#!/bin/bash
# Pre-commit hook: run ref integrity check only when *.md files are staged.
# Install: git config core.hooksPath .githooks

if git diff --cached --name-only | grep -q '\.md$'; then
  exec "$(git rev-parse --show-toplevel)/.claude/tools/check-ref-integrity.py"
fi
